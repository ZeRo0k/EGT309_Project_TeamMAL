apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-model
spec:
  capacity:
    storage: 100Mi # Adjust if needed
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: "/mnt/data/saved_model" # Standardized path
    type: DirectoryOrCreate
